require 'spec_helper'

describe Grenache::Ruby::Ws do
  it 'has a version number' do
    expect(Grenache::Websocket::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
